import React from 'react'
import Products from'./components/Products';

export default function Products() {
  return (
    <div>
      const [url,SetUrl]=useState("http://20.244.56.144/test/companies/:companyname/categories/:categoryname/products?top=n&minPrice=p&maxPrice=q");

    let arr[]=

      forEach(let i=0;i<url.length;i++)
      {

      }

    </div>
  )
}
