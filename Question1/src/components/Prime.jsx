import React from 'react'
import {useState} from 'react'

export default function Prime() {
  const [avg,SetAvg]=useState(0);
  return (

    <div>
        let url="";
      arr[] =[2,4,3,30,29];

        forEach(let i:arr)
        {
            SetAvg(avg+i)
        }

        console.log(avg//arr.size())

    </div>
  )
}
